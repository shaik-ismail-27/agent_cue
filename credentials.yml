rest:
  # CORS settings to allow GitHub Codespaces to connect from browser
  cors_origins:
    - "*"
  enable_api: true

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true
  cors_allowed_origins: "*"

rasa:
  url: "http://localhost:5002/api"
